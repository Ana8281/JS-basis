const msg = 'smart tv has a price of 1000';
console.log(msg.toLocaleUpperCase());
console.log(msg.toLocaleLowerCase());


//SUMMARY

const sringMethods = `length,
    concat,
    literals,
    +,
    ",",
    trim,
    starttrim,
    endtrim,
    index,
    includes,
    slice,
    substr,
    replace,
    repeat,
    split,
    toUpperCase,
    toLowerCase
`;
