const name = 'ana';
const price = 50;
const available = true;

//an object groups everything into a single variable

//object literal

const product = {
    name: 'ana',
    price: 50,
    available: true
}

console.log(product);