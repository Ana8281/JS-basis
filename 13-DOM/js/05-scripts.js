const cards = document.querySelectorAll('.card');
console.log(cards)

const forms = document.querySelectorAll('#formulario')
console.log(forms)

//if query selectorAll doesnt exist it loads as nodelist empty

const notExist2= document.querySelectorAll('not-exits')

console.log(notExist2)
